<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sports">
	
	<!-- 회원 가입 -->
	<insert id="register" parameterType="UserVO">
		INSERT INTO SPORT_USER (USER_EMAIL, nickname, password, phone_number, gender, image)
		VALUES (#{user_email}, #{nickname}, #{password}, #{phone_number}, #{gender}, 
		<choose>
            <when test="image != null">
                #{image}
            </when>
            <otherwise>
                NULL
            </otherwise>
        </choose>
		)
	</insert>

	<!-- 로그인 -->
	<select id="authenticateUser" parameterType="map" resultType="UserVO">
	    SELECT * FROM SPORT_USER 
	    WHERE USER_EMAIL = #{user_email} AND password = #{password}
	</select>
	
</mapper>














